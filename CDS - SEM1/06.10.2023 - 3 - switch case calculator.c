/* WAP using switch case that reads 4 numbers and displays menu that has 4 operations : 
calculate total, calculate average, display smallest, display largest */

#include <stdio.h>
void main(){
	float a,b;
	char s[1];
	printf("Enter the operation to be performed : \n");
	printf("(* product, + sum, - difference, / quotient, % remainder)");
	scanf("%s",s);
	switch(s){
		case '+':
			int(6.7)%int(5.2) 
			 
	}
}